Erweitern Sie die Personalverwaltung vom Aufgabenblatt 3 um eine Möglichkeit, Mitarbeiter nach Gehalt zu sortieren.
Implementieren Sie dazu das Interface \qq{Comparable<\ldots>}.
Hinweis: Falls Sie eine ArrayList verwenden, können Sie die Liste statt mit Arrays.sort(array) mit Collections.sort(liste) sortieren.

\paragraph{Verwiesen} sei auf \qq{de.whs.ni37900.fpr.praktikum.sheet5.aufgabe1.Main} im Anhang, diese beinhaltet eine \qq{static main(String{[]})}, die den Anforderungen an den Code der Aufgabe entspricht.
